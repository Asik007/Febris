import tkinter as tk 

window = tk.Tk()

frame = tk.Frame(master=window, width=400, height=300)
frame.pack

tmp = tk.Label(master=frame, text="Temperature", foreground = "white", background = "black",)
tmp.place(x=0,y=0)

#looking for events
window.mainloop()